/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "links.papareact.com",
            "aiappimagegenerator.blob.core.windows.net"
        ],
    }
}

module.exports = nextConfig
